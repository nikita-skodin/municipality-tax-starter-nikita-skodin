spring:
  flyway:
    locations: classpath:db/migration
  datasource:
    username: nikita
    password: nikita
    url: jdbc:h2:mem:Desktop
    driverClassName: org.h2.Driver
  jpa:
    database: H2
    hibernate:
      ddl-auto: validate
  h2:
    console:
      enabled: true
      settings:
        web-admin-password: root